a  = [10, 20, 20, 30, 30, 40, 50]
dic = {}
for i in a:
    if (i in dic):
        dic[i] += 1 
    else:
        dic[i] = 1 
mode = []
max_count = max(dic.values())
for i,j in dic.items():
    if (j == max_count):
        mode.append(i)
print(mode)
