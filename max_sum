def fun(lists):
    current_sum = lists[0]
    max_sum = lists[0]
    for i in lists[1:]:
        current_sum = max(i,current_sum+i)
        max_sum = max(current_sum,max_sum)
    return max_sum 
a = [-2,1,-3,4,-1,2,1,-5,4]
print(fun(a))
